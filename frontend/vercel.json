{
  "installCommand": "npm install && npm install --prefix ../backend && npm exec --prefix ../backend prisma generate",
  "buildCommand": "npm run build --prefix ../backend && npm run build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  }
}
